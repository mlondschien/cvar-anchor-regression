name: cvar-anchor-regression
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.12
  - numpy
  - scipy
  - scikit-learn
  - pip  # build only
  - setuptools  # build only, as CI installs with --no-build-isolation
  - pytest  # tests only
  - pytest-cov  # tests only
  - ivmodels  # tests only